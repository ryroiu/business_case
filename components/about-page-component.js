export default {
  setup() {
    const route = VueRouter.useRoute(); const store = Vue.inject('itemsStore');
    return { route, item: Vue.computed(() => store.items.find(item => item.id === route.query.service)), resourceName: Vue.computed(() => ({ runbook: 'Sample runbook', 'on-call': 'Sample on-call schedule', repository: 'Sample repository' })[route.query.resource]) };
  },
  template: `<section class="container page-space reading-width"><template v-if="resourceName"><p class="eyebrow">FICTIONAL RESOURCE</p><h1 tabindex="-1">{{ resourceName }}</h1><p class="intro-copy">{{ item ? item.name : 'Example service' }}</p><div class="notice"><strong>Demonstration only.</strong> This sample confirms where the catalog link leads. No operational system is connected.</div><div class="detail-panel mt-4"><template v-if="route.query.resource === 'runbook'"><h2 class="h4">A guide for the responding engineer</h2><p>A real runbook would describe symptoms, diagnostic checks, recovery procedures, and escalation contacts reviewed by {{ item ? item.location : 'the owning team' }}.</p><p class="mb-0">This fictional guide contains no production commands.</p></template><template v-else-if="route.query.resource === 'on-call'"><h2 class="h4">{{ item ? item.location : 'Owning team' }} rotation</h2><p>A real schedule would identify the current primary and backup responders in the team's incident tool.</p><p class="mb-0">No current responder is shown or contacted in this prototype.</p></template><template v-else><h2 class="h4">Source and review history</h2><p class="mb-0">A real repository link would open this service's source code, documentation, and reviewed changes.</p></template></div><router-link v-if="item" :to="'/items/' + item.id" class="back-link mt-4">← Back to {{ item.name }}</router-link></template><template v-else><p class="eyebrow">ABOUT SIGNPOST</p><h1 tabindex="-1">A shared starting point.</h1><p class="intro-copy">A small catalog for finding the people and information behind a service.</p><div class="detail-panel"><h2 class="h4">Built around ownership</h2><p>Signpost is a fictional internal service catalog for Alder Systems. Browse services, find the accountable team, and follow the runbook or on-call schedule.</p><p>All eight services are examples. This public demonstration contains no real internal systems or live incident data.</p><h2 class="h4 mt-4">Keeping information useful</h2><p>For a real catalog, propose a correction in the source repository. The owning team reviews the change; the catalog steward publishes it and coordinates quarterly checks. This read-only app does not edit records or enforce that review process.</p><p>Entries more than 90 days past review are marked overdue. Dates do not prove that a link works or that a service is healthy.</p><h2 class="h4 mt-4">Service tiers</h2><ul><li><strong>Tier 1 · Critical:</strong> core customer journeys stop if unavailable.</li><li><strong>Tier 2 · Important:</strong> significant functionality is impaired.</li><li><strong>Tier 3 · Supporting:</strong> limited immediate customer impact.</li></ul><h2 class="h4 mt-4">Your pins</h2><p class="mb-0">Pins are saved in this browser. They do not sync with other devices or change the shared catalog. Clearing browser storage removes them.</p></div><div class="mt-4"><a href="docs/design/specification.md">Read the specification</a> · <a href="docs/design/business-case.md">Read the business case</a></div></template></section>`,
};
