# Review and refinement brief — Signpost

Compare the first plan and tasks drafts against the specification, the two course guides, and the current source. Preserve the drafts before editing. Record the actual changes in `docs/design/planning-review.md`.

1. Add a concrete baseline inventory with its source revision and the date of historical test evidence. Make it clear that pending acceptance tasks do not mean the corresponding feature is missing.
2. Tighten review-date tests to exactly 90 versus 91 days, impossible/future dates, month/year boundaries, and a recorded timezone.
3. Remove the apparent dependency between early pinned-only filtering and the later pin-control test by using a seeded test pin.
4. Include optional images on detail and delayed-load/recovery focus in acceptance checks. Check recognition of the current Services section on a detail screen.
5. Separate data-loaded filter performance from initial network-dependent startup; specify measurements rather than claiming targets already pass.
6. Allow tester scheduling after the baseline review, while keeping actual sessions dependent on the technical checks. Require newly discovered correction/retest tasks to become release dependencies.
7. Verify full requirement coverage, task ordering, estimate totals, links, and course headings. Preserve pending human sign-off and actual user evaluation rather than manufacturing completion.

Draft a three-part reflection based on this recorded process and append the two required public document links. Identify it as a draft for the author's own review and personalization.
